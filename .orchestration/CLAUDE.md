# Shared Brain (CLAUDE.md)

Persistent knowledge for parallel sessions (Architect/Builder/Tester). Append lessons when verification fails or architectural decisions are made.

- Prefer select_active_intent before any write when .orchestration/ exists.
- Use mutation_class AST_REFACTOR for refactors, INTENT_EVOLUTION for new features.
